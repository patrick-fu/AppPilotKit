{
  "name": "@apppilotkit/protocol-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Executable AppPilotKit wire-contract fixtures",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  },
  "engines": {
    "node": ">=20"
  }
}
